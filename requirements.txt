# --- Ядро ---
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
pyarrow>=15          # быстрый parquet для хранения свечей
python-dotenv>=1.0
pyyaml>=6.0          # configs/accounts.yml (broker creds), bot/accounts_config.py

# --- Данные ---
ccxt>=4.3            # крипта (если понадобится)
yfinance>=0.2        # быстрый доступ к котировкам для прототипов

# --- Бэктест ---
vectorbt>=0.26       # быстрый скрининг идей / перебор параметров
# nautilus_trader>=1.200   # основной event-driven движок (fut/forex). Ставится отдельно, тяжёлая сборка.

# --- Метрики / анализ ---
quantstats>=0.0.62   # готовые метрики и отчёты по equity
ta>=0.11             # технические индикаторы

# --- Dev ---
pytest>=8.0
ruff>=0.5
jupyterlab>=4.0
