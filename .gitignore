# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Envs
.venv/
venv/
env/
.python-version

# Data & outputs (не коммитим тяжёлые данные и сгенерированные отчёты)
data/raw/*
data/processed/*
data/histdata/*
data/fresh/*
data/GER40/*
!data/**/.gitkeep
reports/*
!reports/.gitkeep

# Secrets
.env
*.key
secrets.*
accounts.yml

# OS / IDE
.DS_Store
.idea/
.vscode/

# Claude Code local machine settings (per-machine permission grants, not shared)
.claude/settings.local.json
data/app.db

# Scratch holding pen for files pending manual deletion (device_bash cannot rm)
_to_delete/
