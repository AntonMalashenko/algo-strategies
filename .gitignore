# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Envs
.venv/
venv/
env/
.python-version

# Data & outputs (heavy data files and generated reports are not committed)
data/raw/*
data/processed/*
data/histdata/*
data/fresh/*
data/GER40/*
data/.scheduler_locks/
!data/**/.gitkeep
reports/*
!reports/.gitkeep

# Secrets
.env
*.key
secrets.*
accounts.yml

# OS / IDE
.DS_Store
.idea/
.vscode/

# Claude Code local machine settings (per-machine permission grants, not shared)
.claude/settings.local.json
data/app.db
data/app.db.bak-*
data/state/
# ALGODEV-45 step 1: scripts/s007_watchdog.py runtime artifacts -- the
# sentinel alert file and its dedupe-state, not source.
data/watchdog_alerts.log
data/.s007_watchdog_state.json

# Machine-local agent skill mirrors / staging artifacts (not shared)
.agents/
_deploy_incoming.tgz
_stage_tmp.tar.gz

# Scratch holding pen for files pending manual deletion (device_bash cannot rm)
_to_delete/
