# Runtime dependencies for the webapp/runner Docker image (bot/ + webapp/ + utils/
# only). Deliberately separate from requirements.txt, which is the research/dev
# stack (jupyterlab, vectorbt, matplotlib, ccxt, yfinance, ...) not needed here.
# Versions pinned from the working local .venv (`pip freeze`) -- keep in sync
# manually when a dependency is added to bot/webapp/utils.

# --- data / strategy engine (imported by bot/, strategies/ used at runtime) ---
pandas==3.0.3
numpy==2.4.6
PyYAML==6.0.3
python-dotenv==1.2.2
requests==2.32.3

# --- webapp (FastAPI control panel + DB) ---
fastapi==0.139.2
starlette==1.3.1
pydantic==2.13.4
uvicorn==0.51.0
Jinja2==3.1.6
python-multipart==0.0.32
itsdangerous==2.2.0
SQLAlchemy==2.0.51
alembic==1.18.5
cryptography==42.0.8

# --- cTrader broker adapter (bot/ctrader.py, bot/ctrader_s007.py) ---
ctrader_open_api==0.9.2
Twisted==24.3.0
# cryptography/pyOpenSSL/service-identity left unpinned (floor only): the
# exact local-venv trio (cryptography==42.0.8 + pyOpenSSL==24.1.0 +
# service-identity==26.1.0) is mutually UNRESOLVABLE from a clean install --
# pyOpenSSL 24.1.0 caps cryptography<43, service-identity 26.1.0 needs
# cryptography>=47 (found building this image; the local venv apparently
# predates that conflict and was never fully re-resolved). Let pip pick a
# self-consistent set instead of re-guessing exact numbers.
cryptography>=42
pyOpenSSL>=24.1.0
service-identity>=24
